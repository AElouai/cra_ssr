import React, {Component} from 'react';
import {BrowserRouter, View} from '../routes'
import './index.css';

class App extends Component {
    render() {
        return (
            <BrowserRouter>
                <View />
            </BrowserRouter>
        );
    }
}

export default App;
